/**
 * Given an object a, assuming that the object already have prototype c.
 * Given another object b, how do you make object b become prototype of object a instead of c
 */


/**
 * Given an object a, assuming that the object already have prototype c.
 * How do you add some new properties to prototype c and allow a to use it
 */

/**
 * Given an object a, assuming that the object already have prototype c.
 * How do you update some old properties of prototype c and allow a to use it
 */